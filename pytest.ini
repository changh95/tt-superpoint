[pytest]
# Pins the pytest rootdir to code/ so ./conftest.py (device / device_params fixtures, --device-id)
# is loaded no matter where pytest is invoked from. Tests live in models/tests.
testpaths = models/tests
